import 'package:flutter/material.dart';

class ServoPage extends StatelessWidget {
  const ServoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("this is a servo page"),
    );
  }
}