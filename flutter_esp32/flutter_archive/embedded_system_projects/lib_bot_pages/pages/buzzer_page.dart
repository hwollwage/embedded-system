import 'package:flutter/material.dart';

class BuzzerPage extends StatelessWidget {
  const BuzzerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("this is a buzzer page"),
    );
  }
}