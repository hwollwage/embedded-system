import 'package:flutter/material.dart';

class LedPage extends StatelessWidget {
  const LedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("this is a LED page"),
    );
  }
}